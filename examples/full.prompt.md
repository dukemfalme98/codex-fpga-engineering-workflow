# FULL example

```text
Use $run-fpga-workflow in FULL mode to implement an asynchronous streaming datapath. First confirm ports, clock/reset relationship, throughput, latency, FIFO behavior, backpressure, error recovery, target, and acceptance evidence. Run CDC/timing, verification, and vendor pre-review in parallel; use one product writer; use stable-diff checkpoints; isolate every EDA job; re-review the integrated diff and obtain independent sign-off. Do not guess missing hardware facts.
```
