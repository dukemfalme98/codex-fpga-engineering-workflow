interface:
  display_name: "FPGA Engineering Workflow"
  short_description: "Single-writer FPGA design with native toolflow evidence"
  default_prompt: "Use $run-fpga-workflow with stable artifacts, native project/par and simulation/work toolflows, isolated codex_out diagnostics, bounded repair, and independent sign-off."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
policy:
  allow_implicit_invocation: true
